#Managing_remotes #git
### Managing remotes
- ```git remote```
	- Shows remote repos 
-  ```git remote add upstream url```
	-  Adds a new remote called upstream 
-  ```git remote rm upstream```
	- Remotes upstream